{
  "name": "weekday-calendar",
  "private": true,
  "workspaces": [
    "apps/**",
    "packages/**"
  ],
  "packageManager": "bun@1.2.9",
  "scripts": {
    "build": "turbo build",
    "clean": "git clean -xdf node_modules",
    "db:generate": "turbo -F @weekday/db generate",
    "db:push": "turbo -F @weekday/db push",
    "db:studio": "turbo -F @weekday/db studio",
    "dev": "turbo dev",
    "dev:next": "turbo dev --filter=@weekday/web",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  },
  "dependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
